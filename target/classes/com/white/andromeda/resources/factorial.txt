//Программа по вычислению факториала

x = 12;             //Переменная, факториал которой будет найден
for (fact = 1; x > 0; x -= 1)      //Цикл для факториала
    fact *= x;
print fact;

// Код для демонстрации новой if-else конструкции
/*
    if (x > 3){
            if (fact < 1000)
                print x;
        }else
            print fact;

    a = -1;
    print a; //своего рода разделитель для удобства
    x = 5;

    // Код для демонстрации нового While
    while (x == 5)
        if (1){
            print fact;
            x -= 1;
        }
    print x;
    //Просто код, для демонстрации реализации условий
    if (x != 0 < 1){
        if (x > 0){
            print x;
        }
        print x;
    }

*/